exec my_commands.cfg

bind q switch
bind MOUSE5 +hold

bind MOUSE3 +use_action_slot_item

bind k kill
bind l explode

bind MOUSE4 +reload
bind tab +scores

bind 1 "load_itempreset 0"
bind 2 "load_itempreset 1"

bind p vm_toggle

bind i "next_map_vote 0"
bind o "next_map_vote 1"
bind p "next_map_vote 2"
